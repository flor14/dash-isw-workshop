name: base
channels:
  - conda-forge
  - defaults
dependencies:
  - dash
  - jupyter-dash # even if I call it with _
  - pandas
  - jupyterlab-dash=0.3.0
  - python=3.8
  - plotly
prefix: C:\Users\Florencia\miniconda3
