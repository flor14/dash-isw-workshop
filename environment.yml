name: base
channels:
  - conda-forge
  - defaults
dependencies:
  - dash
  - jupyter-dash # even if I call it with _
  - pandas
  - jupyterlab-dash
  - python=3.9
  - plotly
prefix: C:\Users\Florencia\miniconda3
